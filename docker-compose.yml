version: '3.8'

services:
  app_concursos-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app_concursos-react
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
