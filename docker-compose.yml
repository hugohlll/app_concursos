version: '3.8'

services:
  quiz-app-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quiz-app-react
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
